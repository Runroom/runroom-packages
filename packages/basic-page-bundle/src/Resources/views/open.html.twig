{% if object.publish %}
    <a href="{{ path('runroom.basic_page.route.show', {'slug': object.slug}) }}" class="btn btn-sm btn-default" target="_blank">
        <i class="fa fa-external-link"></i> Open
    </a>
{% endif %}
